#include <stdio.h>

int main ()
{
    int a, i;
    scanf("%d", &a);
    for (i=2; i<a; ++i)
    {
        if (a%i==0) {
        printf("%d nie jest liczba pierwsza", a);
        return;
        }
    }
    printf ("%d jest liczba pierwsza", a);
    return 0;
}
